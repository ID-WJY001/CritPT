# Data

Only tiny public/example data should be committed here.

- `seeds/critpt_seed.jsonl`: one toy seed example used for verifier smoke tests.
- `eval/critpt_example_eval.jsonl`: small public-style evaluation examples.

Generated train/validation parquet files, model rollouts, and large JSONL
datasets are intentionally ignored or kept outside Git.

